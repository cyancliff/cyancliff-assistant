# CyanCliff Assistant

把文档变成**可引用、可核验**的 Markdown。

**依赖很少，但不是零**（这句原先写的是"零依赖"，而它已经不对了 —— 2026-09-19 修正）：

| 部分 | 依赖 |
|---|---|
| 文档工具（`cite-check` / `pdf-to-md` / `docx-to-md` / `find-quote`） | **Node 内置模块 + PyMuPDF**，没有别的 |
| 能力组合（`workflow` / `mail-triage`） | 同上 —— 端口可注入，自测不需要凭据 |
| **飞书 bot**（`feishu-*`） | **`@larksuiteoapi/node-sdk`**（唯一一个运行时依赖，约 30MB） |

飞书长连接是私有协议，只能用它官方的 SDK —— 自己实现等于自己写验签与重连。
这也是"需要一个依赖"和"需要一个公网端点"之间的取舍，见 `PLAN-FEISHU-BOT.md`。

---

## 解决的问题

让 AI 摘要一份材料很容易。但**一份漂亮的摘要，不能证明阅读真的发生过** ——
更麻烦的是引用：模型给出的"出处"，你没法确认那句话到底在不在原文里。

文档工具（下面那三个）围绕一个观察：

> 既然引用的是**原文**，那么"引用能不能在原件里被重新找到"
> 就是一个**可以程序化验证**的性质。

于是"出处可查"从一句承诺变成一条命令。

---

## 它现在是什么（2026-09-19 重写）

这个仓早就不是"三个文档工具"了 —— 而 README 一直还那么写着。
现在按**能力**列，每项都标明**它做到哪、什么还没做**：

| 能力 | 干什么 | 状态 | 依赖 |
|---|---|---|---|
| **文档 → 可引用 Markdown** | PDF/DOCX 转 md + 引用核验 | ✅ 完成，真实资料验过 | PyMuPDF |
| **个人资料库** | 编目、检索、引用可核对 | ✅ 完成（索引是**手工**的） | — |
| **邮件** | 取信（幂等）、拟草稿、确认后发送 | ✅ 完成，真发过 | Gmail API |
| **重要性漏斗** | 判断哪些邮件值得打断你 | ⚠️ **规则层 + 历史层完成；模型层没做** | — |
| **飞书** | 推送 + 可交互 bot（`/` 命令、按钮确认） | ✅ 完成，真连过 | 飞书 SDK |
| **能力组合** | 取信 → 拟稿 → 推手机 →（你确认）→ 发送 | ✅ 编排完成 | — |
| **发布到网站** | 草稿 → 网站内容目录（写入 → verify → 失败还原） | ✅ 完成 | 网站仓 |
| **工程门** | 语法门、契约审计、分层提交/推送门、CI | ✅ 本地两道门实测；**CI 已跑过，抓到两处干净机器上才暴露的问题，已修** | — |

下面几节讲的是第一项（文档工具）——那是这个仓的起点，现在只是其中一块。

---

## 文档工具

### `cite-check.mjs` — 引用核验器

扫描条目文件里的引用，检查每一条能不能在资料里重新找到。

```bash
node scripts/cite-check.mjs                 # 扫描 docs/
node scripts/cite-check.mjs docs/某条目.md   # 只检查一个文件
node scripts/cite-check.mjs --explain       # 打印引用格式
node scripts/cite-check.mjs --scan notes --root .
```

引用格式：

```markdown
library/某论文.md:42                文本原生资料
“引用原文”（library/某论文.md:42）   带到行号，引文可以跨行
（library/某论文.pdf 第7页）          只到原件，没有 md
```

它会区分三种失败，因为**修法完全不同**：

| 报出 | 含义 |
|---|---|
| 引文和第 N 行**不完全一致** | 行号对了，但字符有差异（上下标、全角半角） |
| 引文**不在第 N 行**，实际在第 M 行 | 行号漂移了，引文本身没问题 |
| 引文**整份资料里都找不到** | 这条引用是坏的，不要当它成立 |

细节：

- **比对时空格宽松、字符严格。** 不换行空格、全角空格、普通空格视为同一个
  （`0.5` 后面跟不换行空格还是普通空格，是同一句话，肉眼也看不出差别）；
  但 `σ02` 和 `σ²₀` 算不同字符。
- **代码围栏和反引号行内代码会被跳过。** 否则文档自己的格式示例会被拿去核对。
  代价是：**引用包进代码里 = 放弃核验**。
- 围栏之外的示例可以用 `<!-- cite-check:off -->` … `<!-- cite-check:on -->` 圈起来。

### `pdf-to-md.py` — PDF 转 Markdown

面向"有文字层"的 PDF（不是扫描件）。

```bash
python scripts/pdf-to-md.py library/某论文.pdf
```

处理了两件容易出错的事，都是实测撞出来的：

- **span 之间的空格重建.** PyMuPDF 会把一行切成大量小 span（引用编号、
  逗号各自成 span）。直接拼接文字，`English` + `to` + `-German` 会粘成
  `Englishto-German`。按 x 坐标判断该不该补空格。
- **行尾连字符一律保留.** PDF 里行尾连字符混合了**真实断词**
  （`transduc-`/`tion`）和**本来就有连字符的复合词**（`English-`/`to-German`）。
  没有字典就分不清，而猜错会把复合词粘错 —— 那是主动污染正文。
  保留只是留下一个可见的断词痕迹（`transduc-tion`），不损坏任何文本。

### `docx-to-md.py` — Word 转 Markdown

```bash
python scripts/docx-to-md.py "某论文.docx"
python scripts/docx-to-md.py "某论文.docx" --redact library/某论文.redact.json
```

docx 是 zip + XML，**Python 标准库就能读**，不需要装 python-docx。

按段落样式映射标题层级（章 / 节 / 小节），并**支持脱敏**：
论文封面常带学号这类不该被随手引用的信息，`--redact` 在**写文件之前**替换掉，
产出里根本不存在原值。规则文件里写的是正则，所以它自己也不留原值。

> 样式名到标题层级的映射是按常见 Word 论文模板写的。
> **换模板要重新核对，不要以为它通用。**

---

## 目录约定

```
docs/       条目文件：一份文档一个，写它的保真度和引用
library/    资料：原件 + 同名同前缀的转换产物（.md 进版本管理，原件不进）
scripts/    所有工具（不止文档那几个，见上面的能力矩阵）
PLAN.md     待办与决策的唯一入口
RELEASING.md 发布清单（只写实测过的命令）
```

**md 与原件同名同前缀**是硬要求 —— 这样从引用能直接推出原件路径，不用查表。

---

## 这套东西的边界

说清楚，免得把"绿了"当成超过它实际含义的东西：

- **`cite-check` 只保证到 md 这一层。** 它不判断 md 是否忠实于 PDF 原件 ——
  那需要解析器。所以「原文」类可引用性是**弱承诺**：引文在 md 里找得到，
  不等于 md 忠实于原件。
- **转换不保证忠实。** 表格结构基本会丢，公式上下标会降级，双栏顺序可能串，
  图片只剩图注。这些都在条目里如实记录，并且**按类别降级**
  （比如「可引用: 原文（正文）／仅页码（表格、公式）」），而不是笼统写"原文"。
- **原始资料永远不要删。** 派生品可以重做，原件不能。
- **重要性漏斗只按主题与发件人判断，不读正文。** 这是有意的（防提示注入），
  代价是漏报与误报都会发生 —— 所以每条判定都带理由，让你能看出它凭什么这么判。
- **未确认的草稿发不出去**，而且确认之后**收件人、主题、正文任何一处被改过都作废**。
  这是机制不是劝告。

---

## 权限

| 服务 | 申请了什么 | 用来干什么 |
|---|---|---|
| Gmail | `gmail.readonly`、`gmail.send`、`gmail.modify` | 读信 / 发信 / 改标签与移进回收站（**不含永久删除**） |
| 飞书 | 单聊消息读取、以应用身份发消息 | `/` 命令与推送（卡片回调**不需要**额外权限） |

凭据只从 `.env` 读，永不进版本管理、日志或提交信息。两个仓各有一个 `pre-push`
钩子挡住误推。

---

## 环境

- Node >= 22.12
- Python >= 3.10 + PyMuPDF（`pip install pymupdf`）—— 只有文档转换需要
- 唯一一个 npm 运行时依赖：`@larksuiteoapi/node-sdk`（飞书长连接）

## 怎么验证它没坏

```bash
npm test              # 全部自测（不需要凭据），当前 333 项
npm run gate          # 提交门：语法 + 契约审计 + 引用核验（约 2 秒）
npm run gate:push     # 推送门：上面全部 + 全量自测 + 契约全量测量
npm run contract      # 只跑契约审计：文档里的数字与引用 vs 实测事实
```

> 那个 333 是**跨机可复现**的口径（各仓级自测组之和）。钩子组 8 项与凭据组只在
> 装了钩子的本机成立，所以**不进这个数** —— 混进去就会让同一条命令在不同机器上报
> 出不同值，而"权威值"必须是换台机器也算得出来的那个。

`npm run test:mutations` 会**故意破坏代码**看自测抓不抓得住 ——
它是唯一能证明"断言不是恒真的"的办法，所以不在提交门里（它要求工作区干净）。
