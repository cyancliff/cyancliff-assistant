# RELEASING.md — 怎么发布一个版本

> **这份文件里的每条命令都实测过**（2026-09-19 发 `v0.1.0` 时跑的）。
> 没跑过的命令不写进来 —— 那正是 `AGENTS.md` 第 3 节那次假契约的教训。

---

## 0. 为什么要有版本号

在这个文件之前，版本号是**三处说法互相矛盾**的状态：

| 处 | 说的 |
|---|---|
| `package.json` | `0.1.0` |
| `CHANGELOG.md` | "还没有发布过版本，所以按日期分节，不编版本号" |
| `README.md` | "零依赖"（与 `package.json` 的 `dependencies` 矛盾） |

而 `0.1.0` 这个号**从没被发布过、也没有对应的 tag** —— 一个没有对应发布的版本号
就是假的，而假的版本号比没有更难查（有人会拿它去比对上游，然后发现对不上）。

**用户的决定（2026-09-19，D5）**：补一次真发布，让 `0.1.0` 成真。

---

## 1. 发布前（必须全绿，缺一条就别发）

```bash
cd "D:\Agent\CyanCliff Assistant"

npm run gate:push          # 语法 + 契约全量 + 引用 + 全量自测（约 70 秒）
npm run test:mutations     # 故意破坏代码看自测抓不抓得住（要求工作区干净）
```

两个仓都要干净、且都已推送：

```bash
git status --short          # 空
cd "Personal Memory"; git status --short; cd ..
```

---

## 2. 改版本号（**两处，必须同步**）

```bash
# ① package.json 的 version
# ② CHANGELOG.md 的「未发布」节 → 「## [0.1.0] - YYYY-MM-DD」
```

`CHANGELOG.md` 平时**只往「未发布」节里加**，发布时才把它切成版本号与日期。
这样既不给没发布的版本编号，又不用事后翻 `git log` 重写一遍。

**版本号怎么定**：`0.x` 阶段按自己的判断（这个仓没有 API 契约，语义化版本号
主要用来标记"哪一堆改动是一起发布的"）。到 `1.0.0` 之前不承诺兼容性。

---

## 3. 打 tag（本地）

```bash
git tag -a v0.1.0 -m "v0.1.0 — <一句话说这次发布是什么>"
git tag -l -n1                     # 核对：tag 存在且带注释
git show v0.1.0 --stat | head -20  # 核对：它指向哪个提交
```

**两仓各自独立打 tag**（内容不同），但发布说明里互相指明：

```bash
cd "Personal Memory"
git tag -a v0.1.0 -m "v0.1.0 — 记忆与资料"
cd ..
```

私有仓的 tag 号与公开仓**不要求相同** —— 它们的内容不是一回事。
但如果你想让它们对应，就在发布说明里写清"公开仓 v0.1.0 对应私有仓 v0.1.0"。

---

## 4. 推送 tag

```bash
git push origin v0.1.0
cd "Personal Memory"; git push origin v0.1.0; cd ..
```

**推送会过 `pre-push` 钩子**（数据检查 + 质量门）。hook 收得到 tag 推送
（`refs/tags/...` 也在 stdin 里），所以这一步**不会**绕过任何检查。

> ⚠️ 如果你改了 tag（删了重打），推送时必须 `--force`，
> 而那个 `--force` 会**同时绕过"远端已有的这个 tag 是什么"的判断** ——
> 所以宁可换一个新号，也不要重打已经推出去的 tag。

---

## 5. 发布后

- `CHANGELOG.md` 顶部**新开一个「未发布」节**（下一次积累从这里开始）
- 如果这次发布改了行为，`OPERATIONS.md` 跟着改（它只写怎么操作，不写状态判断）
- 发布说明里写明：这次发布对应私有仓的哪个提交

---

## 6. 这份清单本身的维护

- **只写跑过的命令。** 没实测的写法不要放进来 —— 会变成下一个假契约。
- **改流程就改这里**，不要只在对话里说。
- `npm run contract:audit` 会检查 `README.md` / `PLAN.md` / `AGENTS.md` 里的
  数字与引用是否与事实一致；这份文件不在检查范围内（它讲的是流程不是状态），
  但里面提到的脚本与 npm 脚本名**会**被引用检查覆盖到。
