# PLAN.md — 总的待办与决策入口

> **这份文件回答一个问题：现在该做什么、谁做、卡在哪。**
> 2026-09-17 建立。**每一项都标了"为什么它排在这个位置"** —— 顺序是依赖顺序，不是重要性顺序。
>
> 三份计划的关系（各管一段，不重复）：

| 文件 | 管什么 | 状态 |
|---|---|---|
| **`PLAN.md`（本文）** | **总入口**：做什么、谁做、卡在哪 | 活的，随时改 |
| `PLAN-ENGINEERING.md` | 工程管理体系：验证层、版本发布、进度追踪、交接、运维 | 待批注 |
| `PLAN-MAIL-TRIAGE-AUTO.md` | 邮件自动判断 → 重要才推 → 需要回复的自动拟稿 | 待批注 |
| `PLAN-FEISHU-BOT.md` | （在私有仓）可交互飞书 bot | 已完成，待收尾 |

---

## 0. 没有待办了 —— W1 到 W6 全部做完（2026-09-19）

**这一节现在应该被删空。** 它在 09-19 那天是这样用的：把"不等任何人、不需要凭据"
的事按依赖排出来，做完一项删一行。现在五行全删了，历史在 §0.1 与 `CHANGELOG.md`。

**当初排的顺序**（D3 定的）：W1 → W3 → W4 → W5。实际执行时多插了两件：

| 序 | 事 | 结果 |
|---|---|---|
| W1 | 三处过期状态 | ✅ `f58e340`（私有仓）。含一处**没改**的：`OPERATIONS.md:148` 的「单向」在标着"旧"的 webhook 小节里，是对的 |
| W3 | 建 `publish.mjs` | ✅ `1d7c325`。自测进 `npm test`，3 个突变验证过会失败，端到端真跑过（写入真网站 → verify 8 页/145 链接/无死链 → 清理干净） |
| W4 | 工程体系的权威表与验证门 | ✅ `5d993d9`。`contract.mjs`（事实机器测量 + 文档核对）+ `gate.mjs`（提交门 2 秒 / 推送门 70 秒）+ `.github/workflows/gate.yml` + 两个 `pre-commit` |
| W4b | 文档里对不上的数字 | ✅ 由 W4 的契约审计接管。**当初实测 302 项断言 / 17 个突变**（原先文档写 246 / 14）；这两个数后来各自长大（见 §0.1），**所以这里不再追着改** —— 只要它过期，`npm run contract` 就会红，权威值以它为准 |
| W2 | 抽出 `mail-classify.mjs` | ✅ `90925d8`。分类逻辑从今只有一份（445 → 257 行的 `mail-triage`） |
| W5 | 邮件三层漏斗的规则层与历史层 | ✅ `edbf5c7`。`mail-important.mjs`，自测 18 项 + 突变验证 |
| W6 | 版本与发布（D5） | ✅ `7334804`：README 的「零依赖」改掉、`RELEASING.md`、**`v0.1.0` 真发布（本地 tag）** |

**推送上去了；CI 跑过了 —— 连红五次，三处真缺陷都修完了，第六次绿了**（2026-09-20，见 §0.3）。
两个仓的 `main` 与 `v0.1.0` 都已推；`pre-push` 的质量门每次都跑（提交门 + 全量自测 +
契约全量测量），所以它不会被绕过。**但本地门全绿不等于 CI 会绿** ——
五次失败全是"干净机器上没有的东西"，本地永远复现不了：

| 抓到的 | 为什么本地看不见 |
|---|---|
| 一条突变**打在死代码上**，永远抓不住 | 本机 `test:mutations` 我也没跑过（它要求工作区干净，而当时一直是脏的） |
| 钩子自测：`core.hooksPath` 在干净机器上必然没有 | 本机装了它 |
| 钩子自测：CI runner **没有全局 git 身份**，且干净 clone 里**没有私有仓** | ① Windows 的 git 从系统用户名兜底；② 本机有私有仓。两个掩盖都在 |

**而且第一次的"修完预演过"是假的** —— 我在干净 clone 上跑绿了，
但那个 clone 里我自己的脚本配了 git 身份。**"预演通过"要成立，
预演环境就得真的和 CI 一样，而"一样"这件事本身需要证据。**

### 0.1 那些事做完之后，现在的状态

- **契约审计全绿**：跨机可复现的权威值 —— 自测 **333 项 · 突变 23 个**；跟踪文件 47/41（公开/私有，**这两项是本机事实，不参与比对**）
  > 这里的 333 与 09-19 白天写的 302 不是"涨了"，是两个口径：**权威值现在只算跨机组**
  > （钩子组 8 项与凭据组只在本机成立，换台机器就变）。原先那个"各分组之和"
  > 已从 `contract.mjs` 删掉 —— 它把两类事实加在一起，实测同一台机器两次能报出
  > 331 与 339 两个数。理由写在脚本里，不是写在这里。
- **两道本地门挂着**：提交 2 秒（语法 + 契约快审计 + 引用核验）、推送 70 秒（加全量测量与自测）
- **CI：绿了**（2026-09-20，run `35493859031`）—— 它此前从没执行过，第一次真跑**连红五次**，
  三处真缺陷都在自测层，详见 §0.3。**那三次"本机预演通过"都是假的**：
  Windows 的 git 会从系统用户名兜底、我 WSL 的预演脚本又自己配了身份，
  两处掩盖合起来让一个必然失败的断言看起来通过
- 邮件那条线：规则层与历史层做完，**模型层与推送通道还没做**（见 `PLAN-MAIL-TRIAGE-AUTO.md` 阶段 1–4）

### 0.3 CI 第一次真跑：连红五次，三处真缺陷（2026-09-20，第六次绿了）

> **这一节的第一版是错的，我改了它。** 初稿写的是"失败属于平台差异"，还把
> "干净 clone 上预演过"当成已经排除的原因。事实是：**我在 Windows 和 WSL 上
> 都"预演"过，但两次预演都被我自己的环境掩盖了** —— WSL 那次我在脚本里
> 配了 git 身份，Windows 这次则是 git 会从系统用户名兜底。两处掩盖
> 合起来，让一个必然失败的断言看起来通过了。
>
> **教训不是"要在干净 clone 上跑"，而是"预演的环境要真的和 CI 一样，
> 而'一样'这件事本身需要证据"。** 最后定位靠的不是预演，是**让门自己
> 把完整输出打出来**（见下面第 4 条）。

五次运行全红（2 次在 `main`、1 次在 `v0.1.0`、后面 2 次是按诊断改完再推的），
三处真缺陷，**全部集中在自测这一层**：

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 1 | 突变测试 `22/23`，永远差一条 | 那条突变替换的代码**不可达**（`if (!bulk)` 里面问 `if (bulk)`），等价于没改，任何测试都抓不住 | 删掉不可达分支；突变改打真正活着的 `if (!bulk)` 闸门；基准集补 2 条用例让那个分支第一次被走到 |
| 2 | 钩子自测 2 条失败（`core.hooksPath`） | `core.hooksPath` 是**本机 git 配置**，clone 不带它；干净机器上"钩子没装"是事实，不是缺陷 | 按本仓自己的架构（`crossMachine` / `localOnly`）：本机才成立的事实只打印、不参与判定 |
| 3 | 钩子自测 2 条失败（gitlink） | **两个原因叠在一起**：① CI runner 没有 `~/.gitconfig`，`git commit-tree` 报 `fatal: empty ident name`；② `inner=$(git -C 'Personal Memory' rev-parse HEAD)` 在私有仓不存在时是**空的**，`git mktree` 于是造不出树 | ① 用 `GIT_AUTHOR_*` / `GIT_COMMITTER_*` 环境变量自带身份；② 没有私有仓时用本仓 HEAD（钩子判的是路径，sha 指向哪无所谓） |
| 4 | **门自己的报错信息把答案吃掉了** | `gateTests` 只取"含 ✗ 的行"前两条、各截 120 字符 —— 而被测程序自己就会打印带 ✗ 的文案，真正的失败行被挤掉；且完整输出**没落盘**，CI 上"跑一遍看看"做不到 | 按缩进分离"结果行"与"程序输出"，带出「期望/实际」；打印输出末尾 6 行（回答"跑到头了吗"）；失败时落盘 `.dsh/gate-tests-failed.log`，CI 加一步 `if: failure()` 打出来 |

**第 3 条的表现最具误导性**：它看起来像"钩子漏掉了 gitlink"—— 也就是这个钩子
最该拦的那一种泄露。实际是**测试自己没造出东西**：空 sha 喂给 `git mktree`，
树没生成，钩子收到全零 sha 就当成"新建分支"放行。**一个坏掉的测试，
长得和一个漏掉的保护一模一样。**

**第 4 条才是这次能查出来的原因。** 前三次我都在读同一句断在 `.../Person`
的话（那是程序输出的路径，不是失败行），只能靠另起环境猜。
把门的报错修好之后，CI 自己把 `fatal: empty ident name ... not allowed`
打了出来 —— **根因一直就在那儿，只是没被打出来。**

**连带后果一件**：第 2/3 条让最后那步 `突变测试` **整个被跳过**，
CI 连"断言不是恒真的"都没验过，而它恰好是第 1 条的所在地。

**现在的规矩**（也是这一节唯一值得留下的东西）：

- 改完要推的东西，**预演环境必须是 CI 的真实条件**：干净 clone（没有私有仓）、
  **没有全局 git 身份**、没有 `core.hooksPath`。缺一样，"预演通过"就不算证据
- 这条已写进 `memory/rules.md`（私有仓）
- **门的报错信息是基础设施**，不是装饰：它指不准，排查成本就从"读一行"
  变成"另起一个 Linux 环境"

### 0.2 事故：我擦掉了网站仓 8 个未提交改动（2026-09-19）

W3 测试时我用 `git reset --hard HEAD~1` 撤销一个测试提交，把用户**8 个未提交改动**
一起还原成 HEAD 版本。`--hard` 对工作树是无差别的，它不认识"哪些改动是我的"。
**恢复无门**：无悬空对象（没 `git add` 过）、无备份文件、无第二份副本、卷影副本要管理员。

损失范围（对照 `CODE_MAP.md` 09-17 的记录）：其中 `content.config.ts`/`content.ts`/
`site.config.ts` 三处**只是注释与文案，没动逻辑**；`testagent.md` 的
`period` 值回到了 `'2026'`；`og.png` / `og-source.svg` 两组二进制无从重建。

**两条规则进 `memory/rules.md`**（私有仓 `afaf519`）：
撤销自己的动作前先看 `git status --porcelain`；命令的效果范围大于意图范围时停下来换一条。
**`publish.mjs` 也据此改了默认值**：提交从默认变成 `--commit` 才做。


---

## 1. 要你拍板的（不决定就堵住下面的事）

> **D1–D7 已于 2026-09-17 定完**，结论见 §2「已定的决策」。现在只剩 D8。

| # | 决定 | 选项 | 不决定会堵住 |
|---|---|---|---|
| **D8** | **两份计划本身的批注** | 直接在两份文件里改，或在这里说 | 堵住 W4、W5 的具体做法。**目前按"计划原样执行"** —— 你没批注，我就按已写的做，实施中遇到分歧再说 |

---

## 2. 已定的决策（2026-09-17）

> 按规矩：决策定了就从上一节删掉，结论写在这里，细节落进对应计划。

| # | 决定 | 结论落到了哪 |
|---|---|---|
| **D1** | 泄露的密钥**不轮换** | 用户接受该风险。**推论写进 `memory/notes.md`**：装着它的会话记录从"待清理的垃圾"变成长期安全边界 —— 不再扩大暴露面（不复制／不粘贴／不进日志），要清理时先确认里面有什么 |
| **D2** | 「自动回复」= **拟稿 + 推确认** | `PLAN-MAIL-TRIAGE-AUTO.md` §4.1 的红线成立：**发送只能由你点按钮触发**。"不问就发"不在这一版里 |
| **D3** | 顺序 **W1 → W3 → W4 → W5** | 本文 §0。先把说假话的改掉、把断掉的发布通道接上，再建会发现问题的系统，最后做新功能 |
| **D4** | **用 CI**（GitHub Actions） | `PLAN-ENGINEERING.md` §4.2 按原样做。公开仓免费；CI 里不需要任何凭据（现有自测全部设计成不依赖凭据） |
| **D5** | **补一次真发布、打 tag** | `PLAN-ENGINEERING.md` §5：`package.json` 保持 `0.1.0`，走 `RELEASING.md` 清单打出 `v0.1.0`；**顺带改掉 README 的"零依赖"**（现在有飞书 SDK） |
| **D6** | 轮询间隔 **5 分钟** | `PLAN-MAIL-TRIAGE-AUTO.md` §5 方案 A（bot 进程里加定时器）。不上 Pub/Sub |
| **D7** | 汇总通道 **一天一条** | `PLAN-MAIL-TRIAGE-AUTO.md` §3：即时通道发 `urgency: high`，其余攒成每日一条 |

---

## 3. 只有你能做的（我做不了）

| # | 事 | 为什么只能你做 | 什么时候 |
|---|---|---|---|
| Y2 | **验证"非 owner 被拒"** | 验收清单第 6 条从来没在真环境验过（单测覆盖了，但**没有一次真实的非 owner 回调打过它**）。需要你拿另一个飞书账号发一条命令 | 你有空时 |
| Y3 | **实际用三次以上** | 课程 D 项的验收是"你愿意反复使用"，这个我造不出来 | 长期 |
| Y4 | **确认那封"账号被停用"是不是真的** | Anthropic 发来的 `Your account has been suspended`。如果是真的，它比这个项目里任何代码都重要 | **今天**（如果它还没过期） |
| Y5 | **网站那 8 个未提交改动怎么处理** | `D:\Agent\CyanCliff Web` 的工作树**现在是干净的**（2026-09-19 晚核实）—— 那 8 个改动已不在工作树里，与 §0.2 记的擦除事故是同一件事。留作"确认你没别的地方要提交" | 你想提交时 |

---

## 4. 没核实的（写下来，不猜）

| 事 | 为什么没核实 | 怎么核 |
|---|---|---|
| Gmail 的 refresh token 会不会 7 天过期 | 要看 Google Cloud Console 里 OAuth 应用的**发布状态**（"测试"状态下 refresh token 7 天失效）。**没查过** | 你去 Console 看发布状态；或在 `health` 里报"令牌上次刷新时间" |
| Pub/Sub pull 订阅能否完全不用公网端点 | 文档说可以，**但没实测**。写进邮件计划的 §5 时已按"不确定"处理 | 真要上方案 B 时先打样 |
| `mail-fetch` 轮询在配额上够不够 | 算过（`messages.list` 5 单位 vs 每秒 250），**没实测过持续轮询** | 阶段 4 实跑一天看 |
| `publish.mjs` 写入后网站 `verify` 的实际耗时 | 没跑过网站全量 verify | W3 第一次跑时记下来 |

---

## 5. 明确不做的（免得以后又想加）

| 不做 | 为什么 |
|---|---|
| ehall（办事大厅） | 用户已决定不做（私有仓 `PLAN.md` §34） |
| 自动发送邮件（除非 D2 说要） | `mail-send.mjs` 那两道闸门不能绕 |
| 自动删除/归档邮件 | `modify` 权限够，但"工具给的名字不能当处置依据"（`rules.md`） |
| ESLint / Prettier / 换测试框架 / 重型 CI | 现在的问题不是代码风格，见 `PLAN-ENGINEERING.md` §0 |
| 把 `AGENTS.md` 缩成只剩红线 | 实测过成本：新会话要多两轮才能建立正确认知 |

---

## 6. 这份文件怎么维护

- **做完一项就把那一行删掉**（它的历史进 `CHANGELOG.md`）—— 这份文件只装**还没做的**。
- **决策定了一项就把那一行删掉**，把结论写进对应计划的正文。
- **过期比没有更糟**：这张表如果开始说假话，就重蹈 `AGENTS.md` §3 的覆辙。
  工程体系**阶段 1+2** 做完之后，表里的数字应当能被 `contract:audit` 核对。

---

## 7. 一个命名冲突，已修（留痕）

原先工程计划的阶段编成 **A–F**，而私有仓 `PLAN.md` 里 **A–F 是四项核心能力** ——
于是「D」在两份文档里指两个东西，读的人只能靠上下文猜（有人问过"di 是什么"）。

**已改成「阶段 1–6 + 自解释的名字」。** 规则进 `memory/rules.md`：
要用字母/编号给一组东西命名时，先查这个编号在本项目其它文档里有没有被占用。
**同一个符号指两件事，正是这套文档自己要治的病。**
