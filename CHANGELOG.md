# 改动记录

记的是**这个仓（公开仓：工具与代码）**的改动。

个人数据、记忆与计划的改动在内层私有仓，不进这里。那边有两份记录，分工不同：

| 文件 | 回答什么 |
|---|---|
| `Personal Memory/CHANGELOG.md` | 那份仓里**现在有什么、什么时候进来的** |
| `Personal Memory/PLAN.md` 第 13 节 | **执行过程是怎么走的**（撞到什么、学到什么） |

两边细节**只写一处**，另一处放指针 —— 重复的日志会分叉，而分叉的两份比没有更难查。

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

> **还没有发布过版本，所以按日期分节，不编版本号。**
> 编一个没有对应发布的号，那个号就是假的 —— 而假的版本号比没有更难查
> （有人会拿它去比对上游，然后发现对不上）。

---

## [未发布]

（下一次改动从这里开始积累。发布时把它切成 `## [版本号] - 日期`，见 `RELEASING.md`。）

### 2026-09-20

> 起因是"把积压的 16 个提交推上去"。推之前先跑推送门 —— **它是红的**，
> 于是这批改动变成了"先把红的修绿"。

#### 修复 · 权威表与自测数量对不上（推送门因此是红的）

`test-mutations.mjs`（`40a3e6c`）与 `workflow.mjs`（`d059227`）都加过断言，
但**没人重新测量过 `facts.json`** —— 提交门用的是 `--fast`（拿旧表核文档），
它设计上就查不到"事实本身变了"。于是推送门一跑就报 2 处漂移：

| 权威表记着 | 实测 |
|---|---|
| 突变 17 个 | **23 个** |
| 自测组 `workflow` 21 项 | **30 项** |

`node scripts/contract.mjs --update` 重新测量后写回。这正是那道门存在的理由：
**上一批提交自己就制造了漂移，而只有推送门会发现。**

#### 修复 · `assertionTotal` 是个会漂的"权威值"（已删除）

同一个数字两次测量给出不同值：同一台机器、同一份代码，`--list` 报 **331**，
`--update` 写 **339**。原因不是随机性：

    assertionTotal = 各分组之和
                  = 331（跨机组）+ 8（钩子组，**只在本机成立**）+ 0（凭据组）
                  = 339

而 331 那次是**巧合**：`workflow` 组少 9、钩子组少算 1，两处互相抵消。
也就是说这个"权威值"靠误差相消才对得上 —— 它正是本仓要治的那种东西，
只不过这次犯在检查器自己身上。

- 删掉 `assertionTotal`，只留一个口径 `assertionTotalCrossMachine`（各跨机组之和，= 331）
- 契约的数字核对（`CLAIM_PATTERNS`）与审计输出改为引用它
- 各分组之和仍打印，但**同时写明它含本机项**（`--list` 里标着"别引用"）
- 本机事实那一行现在把钩子组也列出来，读者不必去别处拼

#### 修正 · `PLAN.md` 里已经说假话的三处

- §0：写着"还差一步 `git push origin v0.1.0`" —— 这一步连同 `main` 现在做了
- §0.1：`断言 302 · 突变 17 · 跟踪文件 46/41` 全过期（口径也换了，见上）
- §3 Y5：写着网站仓"有 8 个未提交改动" —— **实测是 0**，工作树干净

#### 修复 · 一条突变打在死代码上（**CI 从第一次跑起就是红的**）

修完上一节、工作区干净之后才跑得动 `npm run test:mutations`（它要求工作区干净），
结果是 `22/23 被抓住`：

    漏斗：没有历史就一律推（不区分机器发的）

追下去发现**漏掉的原因不是"断言不够"**：那条突变替换的是

    if (!bulk) {
      …
      if (bulk) { … }     ← 这里

`!bulk` 里面问 `bulk`，恒假。**这段代码从来没被执行过**，所以这条突变等价于
什么都没改 —— 任何测试都不可能抓住它。这条门又正好排在 CI 的第一步，
于是**CI 从第一次跑起就会红**，而它此前从没跑过，所以没人知道。

- 删掉那段不可达代码（行为不变：机器发件人那半本来就由 ② 与末尾兜住）
- 那条突变改打**真正活着的闸门**：`if (!bulk)` —— "批量地址一律不生成回复"
- 基准集补 2 条用例。原先**没有任何用例能走到那个分支**：两条"批量地址不该回信"
  的用例都在噪声层就出局了。现在一条机器发（期望 high + **不**要回）、
  一条真人发（期望 high + 要回），只差发件人性质 —— 突变往任一方向偏都会被抓住
- 实测确认：把 `if (!bulk)` 改成 `if (true)`，自测报 `2/23 项与基准不符`

#### 修复 · 自测"自报总数"这条规矩漏了一个脚本

上一条改动加完用例后，`contract.mjs --update` 把 `importance` 组记成 **25** ——
比真实用例数（21）多 4。原因是这个脚本**没有自报总数**，而测量函数的设计是
"**脚本自己报的才是权威**，没报才退回数 ✓ 行"（数 ✓ 行会把说明行一起数进去）。

`mail-important.mjs` 补上 `全部通过（23 项）`。**权威值因此从 331 变成 333** ——
这不是"断言涨了"，是原先那个数一直是错的。

#### 修复 · 钩子自测在干净 clone 上必然失败（CI 第二次红的原因）

`main` 与 `v0.1.0` 两次 CI 运行全红，失败步骤都是**推送门 → 全量自测**。
在本机用干净 clone 精确复现后，失败只有两条，都在 `test-hooks.sh`：

    ✗ git 找得到 pre-push 钩子（找不到就是没装）
    ✗ core.hooksPath 指向 .githooks

`core.hooksPath` 是**本机 git 配置**，`git clone` 不会带过来 —— 干净机器上它必然是空的。
而"钩子没装"在那台机器上**是事实，不是缺陷**：CI 从不需要推送。
原先的检查把"必须有"当成了前提，于是干净机器必红。

按本仓自己的架构修（`contract.mjs` 的 `crossMachine` / `localOnly` 是同一条原则）：
**本机才成立的事实只打印、不参与判定**，并且说清这台机器上什么没被验证。
代价写在脚本注释里：**CI 不再验证 `core.hooksPath` 真的挂上了**，
那道验证只在本机跑 —— 装了钩子的机器上照旧验它。

**连带后果**：这两条失败让最后那步 `突变测试` **整个被跳过**，
CI 连"断言不是恒真的"都没验过 —— 而它恰好是上面那条死代码突变的所在地。

#### 修复 · 同一条 gitlink 断言在干净 clone 上也是假的（CI 第四~五次红的原因）

修完上面两处 CI 仍然红。这次没有再靠"本机预演"（前三次预演全是假绿，见下），
而是先把**门自己的报错**修好，让 CI 自己说话 —— 完整日志末尾那六行是决定性的：

    fatal: empty ident name (for <runner@runnervmlun5p...>) not allowed

两个原因叠在一起，都只在 CI 的机器上成立：

1. **CI runner 没有 `~/.gitconfig`。** 这个脚本用 `git commit-tree` 造测试用的树，
   没有身份就报上面那句 → `$c_gitlink` / `$c_env` 全空 → 钩子收到全零 sha
   就当成"新建分支"放行 → 两条断言报「期望 1，实际 0」。
   修法：用 `GIT_AUTHOR_*` / `GIT_COMMITTER_*` 环境变量自带身份 ——
   只作用于这条命令，**不写任何配置文件**。
2. **`inner=$(git -C 'Personal Memory' rev-parse HEAD)` 在私有仓不存在时是空的**
   （干净 clone / CI 的常态），于是 `git mktree` 收到空 sha：
   `fatal: input format error: 160000 commit \tPersonal Memory` —— 树根本没造出来。
   修法：没有私有仓就用本仓 HEAD（钩子判的是**路径**，sha 指向哪个提交无所谓）。

**第 2 条的表现极具误导性**：它看起来像"钩子漏掉了 gitlink"—— 也就是这个钩子
最该拦的那一种泄露。实际是测试自己没造出东西：空 sha 喂进去，树没生成，
钩子拿到全零 sha 就放行。**一个坏掉的测试，长得和一个漏掉的保护一模一样。**

为什么本机一直看不见：Windows 的 git 在身份缺失时会从系统用户名兜底（Linux 直接 fatal），
而本机有私有仓。两个掩盖同时在，缺陷就在两个平台上都"通过"了。

已验证：干净 clone 上 3 项全过；把钩子的 gitlink 规则改坏，测试立刻报 ✗
（**证明修复后的断言不是恒真的** —— 这正是这个项目最怕的那种事）。

#### 修复 · 门自己的报错信息把答案吃掉了（这才是能查出来的原因）

`gateTests()` 原先是一行：

    out.split('\n').filter((l) => l.includes('✗')).slice(0, 2).join(' | ')

两处都错，而且都在最需要它的时候才暴露：

- **它截的是"含 ✗ 的行"，不是"失败的行"。** 被自测测的那些程序自己就会打印
  带 ✗ 的报错文案（`gmail-auth` 的「✗ 凭据文件不是合法 JSON」就是），
  于是摘要里出现的是那两行，真正的失败行被挤掉。
- **它没有带上"下一行"。** 这个仓的自测把「期望 X，实际 Y」打在标题行的**下一行**，
  只取标题行等于把答案丢掉 —— 留下来的是断在 `.../Person` 的一句路径。
- 而且 `npm test` 的完整输出**根本没落盘**，CI 上"跑一遍看看"做不到。

结果是：五次红，我前三次都在读同一句话，最后不得不另起一个 Linux 环境
（WSL + 原生 Node 22）去复现 —— **而根因一直就在那儿，只是没被打出来。**

现在：按**缩进**区分"结果行"与"程序输出"（结果行带缩进，程序输出顶格；
这条判据是实测撞出来的）；带出「期望/实际」；打印**输出末尾 6 行**
（它回答"脚本跑到头了吗"：有收尾那行 = 跑完了，没有 = 死在半路）；
失败时落盘 `.dsh/gate-tests-failed.log`，workflow 加一步 `if: failure()` 把它打出来。
已用「故意把一条基准期望改错」验证过能读出是哪一条。

#### 规矩 · 预演"通过"不算证据，除非预演环境真的和 CI 一样

这一节的上一版写的是"要推的东西先在干净 clone 上跑一遍"。**那条规矩不够，
而且我已经照着它做过三次，三次都是假绿。** 因为"干净 clone"只排除了私有仓和
`node_modules`，没排除另外两件 CI 上没有的东西：

| CI 上没有 | 本机上有什么在兜底 |
|---|---|
| 全局 git 身份 | Windows 的 git 先从系统用户名兜底；我 WSL 的预演脚本又自己配了身份 |
| `core.hooksPath` | 本机装了 |

所以规矩改成：**预演环境要逐条对上 CI 的真实条件，而"对上了"这件事本身要有证据。**
可执行版本：干净 clone（无私有仓）+ **`git config --global --unset user.name/user.email`**
+ 不设 `core.hooksPath`，然后跑 `npm test`、`node scripts/gate.mjs --push`、
`npm run test:mutations`。WSL + 原生 Node 22 是可用的复现环境。

**更值钱的一条**：与其反复搭环境猜，不如**先把工具的报错修到能自己回答问题**。
这次真正的突破不是任何一次预演，是第 4 条那个修复 —— 修完 CI 一次就说了实话。

#### 变更 · 复用既有约定，不新增机制

- `.jspace/`（J-space 账本）加进 `.gitignore`：与 `.dsh/` 同类，是**会话状态**，
  不是这个仓的交付物。它对"这套东西现在是什么"不构成声明，进版本管理
  只会变成又一份会过期的文档
- `README.md`：`320+ 项` → **333 项**（并写明这是跨机口径）；CI 那一行从
  "从未跑过一次"改成"已推、结果未核"

---

## [0.1.0] - 2026-09-19

> 第一次真发布。在这之前 `package.json` 里写着 `0.1.0` 而它从没被发布过、
> 也没有对应的 tag —— 一个没有对应发布的版本号就是假的（见 `RELEASING.md` 第 0 节）。

### 新增 · `publish.mjs` —— 契约里那句描述第一次成真

`AGENTS.md` 第 3 节写着「发布器是 `scripts/publish.mjs`」，而它从没存在过（见下面那条修复）。
现在真建出来了，接线到 `npm run publish`：

- 集合从 `Personal Memory/drafts/<集合>/<slug>.md` 推断，文件名即 URL
- 预检只查 schema 管不了的东西（结构 + `draft: true`）——**字段与类型交给网站的 `astro build` 报**，
  在这里再实现一遍 schema 就是第二个真相源
- 拒绝覆盖网站里有未提交改动的文件
- 写入 → `npm run verify` → 失败**逐字节还原**；`verify` 跑不起来与跑了但失败分开报
- **默认不提交**，加 `--commit` 才提交，且只 `git add` 那一个文件

### 新增 · 契约审计与分层验证门（`contract.mjs` / `gate.mjs`）

起因是那次核对暴露的问题：**没有任何东西会主动问"这还是真的吗"。**
三次同类问题（"永不推送"、"待接通"的凭据表、那个不存在的发布器）都是这个洞。

- `scripts/facts.json` 存**测量出来**的事实；`npm run contract` 重新测量并比对
- 再把文档里谈同一件事的数字与引用抓出来核对 → 不符就指出是哪一行
- 只查描述当前状态的文件；**历史文件（本文件）不查** —— 那里是当时的数字
- 分层门：提交门（语法 + 契约快审计 + 引用，约 2 秒）/ 推送门（加全量测量与全量自测，约 70 秒）
- **语法门**：从 `node --check` 逐个跑（29 个脚本 5439ms）改成 `vm.SourceTextModule`
  只解析不执行（54ms），并保留慢路径兜底
- `.github/workflows/gate.yml`：补本地门给不了的三件（干净机器、不依赖 `core.hooksPath`、
  突变测试要干净工作区）。**不需要任何凭据** —— 这是端口可注入的回报

### 新增 · `PLAN.md` 与 `RELEASING.md`

- `PLAN.md` —— 待办与决策的唯一入口：现在该做什么（按依赖排序）、什么在等拍板、什么只能用户做
- `RELEASING.md` —— 发布清单。**只写跑过的命令**

### 修复 · `AGENTS.md` 第 3 节描述的发布器**从没存在过**

原文写着「发布器是 `scripts/publish.mjs`」，还给了 `npm run publish` 的例子。
核对证据：`git log --all --diff-filter=A -- '*publish*'` 是空的、
`git log -S 'publish' -- scripts/` 也是空的、`package.json` 里没有那个脚本。
**不是建了丢了，是从没建。**

同一次核对还查出三处与事实不符，一并改掉：

| 位置 | 原来写的 | 实际 |
|---|---|---|
| 第 0 节 | 只有代码和文档（22 个文件） | 35 个跟踪文件 |
| 第 2 节 | 模板见外层仓的 `.env.example` | 外层没有该文件，真身在内层私有仓 |
| 第 4 节 | 拿 `ehall` 退课当确认闸门的例子 | `ehall` 已决定不做 |

### 修复 · `README.md` 的「零依赖」

它写着"零依赖：Node 脚本只用内置模块"，而 `package.json` 里
有 `@larksuiteoapi/node-sdk`（飞书长连接必需）。改成按部分说明：
文档工具确实只用内置模块，**飞书 bot 需要那个 SDK**。

### 修复 · 文档里在说假话的地方（三处）

- `OPERATIONS.md` 说「飞书是单向的，确认要在对话里说」—— **是错的**，bot 早就双向了。
- `OPERATIONS.md` 说「两套正在迁移，迁完就删掉第一套」—— 改成待办标记。它不是错的，是还没做。
- `PLAN-FEISHU-BOT.md` 还写着打样「⏸ 等凭据」、bot「没连过真飞书」—— 实际已接通。

同一份文件里第 148 行的「单向」**没有改**：它在明确标着"旧"的 webhook 小节里，
说的是那套东西的性质，是对的。**同一个词两处出现、一处对一处错，因为说的不是同一个东西** ——
修的时候要看清两处各在说什么，别一起改掉。

---

## 2026-09-17

一天里从零建起来的。按四项能力分组，最后是质量保障与安全。

### 新增 · 文档工具（A 个人数据库）

- `pdf-to-md.py` / `docx-to-md.py` —— 把 PDF/DOCX 转成**可引用**的 Markdown。
  零依赖（PyMuPDF 之外不引外部命令），**保真度优先于覆盖率**：
  宁可标成"仅页码、不可引用"，也不给一份看起来能引、实际名词被吃掉的东西。
- `find-quote.mjs` —— 找一段文字在哪，回 `文件:行号`。
  带 `--json`（给别的程序用；解析给人看的彩色文本是一改颜色就静默坏掉的做法）。
- `cite-check.mjs` —— 核验引用是不是真的存在。
  三种失败分开报（字符不一致 / 行号漂移 / 整份资料里都没有），因为**修法不同**。
- docx 表格渲染：`gridSpan`（横向合并）会补出空列。

### 新增 · 手机端（B）

- `notify.mjs` + `notify-lib.mjs` —— 飞书 webhook 推送。
  拆成两个文件是因为 `import` 一个脚本不该把 CLI 跑起来。
- **可交互 bot**（`feishu-bot.mjs` / `feishu-core.mjs` / `feishu-card.mjs` /
  `feishu-commands.mjs` / `feishu-policy.mjs`）—— 走**长连接**，不需要公网地址。
  - 卡片按钮：`确认发送` / `看全文` / `作废`，用 JSON 2.0 结构
  - `/` 命令：`/帮助` `/取信` `/稿` `/找` `/状态` `/停`
  - **没有"发送"命令，永远不会有** —— 发送只能点按钮。命令是打字的、打字会打错
  - 只有 owner 的操作算数；非 owner 与群聊一律拒绝
- `feishu-probe.mjs` —— 打样脚本。存在的理由：SDK 的 README 说长连接不支持回调订阅，
  而它的 `channel.md` 和飞书平台文档说支持。**文档互相矛盾时先打样，别拿它当地基。**

### 新增 · 邮件（C）

- `gmail-auth.mjs` —— OAuth PKCE（桌面客户端没法保守密，PKCE 让偷到授权码也不够）。
- `mail-fetch.mjs` —— 取信。**幂等 key = Gmail message id**，重复检查不重复处理。
- `mail-draft.mjs` —— 拟稿。把原邮件 + 同发件人历史 + `memory/preferences.md` +
  `memory/rules.md` 组装成提示。
- `mail-send.mjs` —— **两道闸门**：
  ① 没有确认记录 → 不发；② 确认之后正文被改过（sha256 不符）→ 不发。
  外加**发送锁**（进程内 try-lock + 跨进程文件锁），防并发下发两封。
- `mail-triage.mjs` —— 从未读里挑出真正需要动作的。分类靠标题与发件人的关键词，
  **不理解正文**，天花板写在文件头里。
- `gmail.modify` 权限 —— 可改标签、移进回收站（**不含永久删除**）。

### 新增 · 能力组合（D）

- `workflow.mjs` —— 取信 → 拟草稿 → 推到手机 → （你确认）→ 发送。
  **端口可注入**，所以"编排对不对"和"凭据有没有"是两件分开验证的事。
  **编排自己不会确认、更不会发送** —— 否则那道闸门形同虚设，
  而这个脚本会跑在无人值守的场景里。

### 新增 · 基础设施

- `proxy.mjs` —— Node 不自动用系统代理，而报错是 `fetch failed`，**完全看不出是代理问题**。
- `setup.mjs` —— 逐项打印还差什么凭据、去哪儿拿、填到哪一行。
- `run-sh.mjs` —— npm 在 Windows 上由 cmd 执行，PATH 里没有 `sh`；去问 git 自己装在哪。
- **两个 `pre-push` 钩子**（`.githooks/`）：私有仓只许推私有远端；
  公开仓把将要公开的每一棵树查一遍，含 `Personal Memory`（**包括 gitlink 那种形式**）。

### 新增 · 质量保障

- `npm run test` —— **一条命令跑完所有自测**：7 组，合计 246 项断言
  （流水线 49 · 凭据 7 · 幂等 7 · 编排 13 · 分类 18 · 飞书 147 · 钩子 5）。
- `test-mutations.mjs` —— **故意破坏代码，看自测抓不抓得住**（17 个突变）。
  存在的理由：这个项目里反复撞上"断言恒真"（看着像检查、其实两个分支相同，
  或者只检查了报告值没检查实际行为）。**写完断言必须想办法证明它会失败。**
- `test-hooks.sh` —— 推送闸门的突变测试：造出**本该被拦的东西**看它拦不拦得住。
  "干净状态下通过"在钩子完全失效时也通过。

### 修复

- **`proxy.mjs` 的 `reallyExit` 闷掉了所有后续 exit 处理器。**
  `process.reallyExit()` 立刻终止进程、不再执行其它 exit 监听器，
  而这个模块是很多脚本的间接依赖、处理器总排在最前面 ——
  于是 `mail-send` 的发送锁**从来没被清过**。
  症状会自愈（陈旧锁超时），所以表现成"偶发的『正在发送中』"。
- **确认发送原本是同步等发信完成的。** 飞书要求卡片回调 3 秒内响应，
  而发信要 spawn 子进程 + 走网络 —— 超时 → 重推 → 卡片被重置。
  症状：**邮件真的发出去了，但卡片一直不变绿**，终端上还没有任何错误。
- **`mail-triage --json` 在 JSON 前面打了一行人看的标题**，调用方解析失败。
  规矩：`--json` 模式下 stdout 必须是纯 JSON，人看的一律走 stderr。
- **中文主题原样写进 `Subject:` 头**（MIME 头里非 ASCII 必须编 RFC 2047），
  发出去是 `Re: Ã©Â£ÂžÃ¤Â¹Â¦ bot ...`。发现纯属偶然：测试邮件发给了自己。
- **自动打开授权 URL 用 `cmd /c start`**，而 Node 只给含空格的参数加引号 ——
  `cmd` 把 URL 里的 `&` 当命令分隔符，浏览器只拿到半截。
  Google 回的是「缺少 response_type」，**指向完全错误的方向**。改用 `rundll32`。
- 命令行参数被静默忽略（`--query` 没用、`--limit 2` 的值被当成命令名）。
- 取信时网络抖动导致个别邮件被静默跳过（同一批扫两次数字对不上）→ 加退避重试。
- 凭据报错的顺序与精度：原来"令牌损坏"和"缺 refresh_token"报同一句话，而修法不同。
- `pdf-to-md` 的标题判据只认一种排版，OCR 文档检出 0 个标题 → 两个条件取或。
- `cite-check --scan` 给成文件时抛 ENOTDIR，改成报人话。

### 安全

- **凭据只从 `.env` 读**，永不进版本管理、日志或提交信息。
- 外层 `.gitignore` 挡住内层私有仓；两个 `pre-push` 钩子做第二层。
- `gmail.modify` 是用户明确要求后加的。**它不包括永久删除** ——
  那要 `https://mail.google.com/`（全权），没有申请。

---

## 这个文件怎么维护

- **改了什么就加什么**，别攒着 —— 攒到最后只能靠翻 `git log` 重写一遍。
- **修复要写清症状，不只写结论。** "修好了退出清理"没用；
  "'正在发送中'偶尔出现，而那个进程早就退出了"才是下次能对上号的线索。
- **不编没发布的版本号。** 真要发版时再开新的一节。
